[project]
name = "meta-support-openenv"
version = "0.1.0"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "gradio",
    "pandas",
    "requests",
    "openai",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"

[tool.openenv]
entry_point = "app:app"
