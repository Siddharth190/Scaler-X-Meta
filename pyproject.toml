[project]
name = "meta-support-openenv"
version = "0.1.0"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "gradio",
    "pandas",
    "requests"
]

[project.scripts]
server = "server.app:app"

[tool.openenv]
entry_point = "app:app"
